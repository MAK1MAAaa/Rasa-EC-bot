# 毕设演示任务书：Rasa-EC-bot 电商智能客服系统

本文档用于 10-15 分钟毕业设计现场演示。目标不是完整讲解所有代码细节，而是用一条连贯业务链路证明系统已经具备“电商业务 + 智能客服 + 商家处理 + 实验支撑”的完整能力。

演示对象：答辩老师、评审老师、同学或项目验收人员。

建议设备：一台运行所有服务的 Windows 主机；如需远端展示，另一台设备通过 Tailscale 访问前端 `5173` 端口。

演示主线：用户登录商城，浏览商品，向智能客服请求推荐，生成下单草稿，查询订单和物流，发起售后申请，再切换到商家端处理订单与售后，最后说明系统架构和 benchmark 结论。

## 1. 演示目标

本次演示需要证明以下能力：

- 商品、店铺、订单、售后等核心电商数据可以完整运行。
- 商品图片和店铺 Logo 均来自本地 `/demo-assets/...`，不会因为外部图片站点不可用影响展示。
- 客服能根据用户需求返回商品推荐卡片。
- 客服能根据购物车生成下单草稿，但不会直接落库，必须由用户确认。
- 客服能查询订单、物流和售后状态，并基于真实数据库数据返回结果。
- 客服能生成售后申请草稿，展示“模型生成建议、用户确认执行”的安全边界。
- 商家工作台能处理待发货订单、售后申请和商品/店铺管理。
- Rasa、LLM/Agent、PostgreSQL、Redis 和 benchmark 各自承担明确职责。

## 2. 演示前准备

### 2.1 服务检查

演示前确认以下服务已启动：

| 服务 | 端口 | 用途 |
| --- | ---: | --- |
| 前端 Vite | `5173` | 商城、客服、订单、商家工作台 |
| FastAPI 后端 | `8000` | REST API、客服编排、业务写入 |
| Rasa Server | `5005` | 规则型客服意图与流程 |
| Rasa Action Server | `5055` | Rasa 业务动作 |
| PostgreSQL | `5432` | 主业务数据库 |
| Redis | `6379` | 缓存、锁、pending action 加速 |
| Ollama | `11434` | 本地 LLM / VLM / embedding |
| vLLM / OpenAI-compatible | `8002` | LoRA 推理服务，按需启动 |

### 2.2 初始化检查

数据库使用演示初始化数据：

```powershell
cd backend
.\scripts\init_postgres.ps1
```

初始化后应满足：

- 有 4 个中文店铺。
- 有 20 个演示商品。
- 商品图和店铺 Logo 都使用 `/demo-assets/...` 本地路径。
- `test1@example.com` 有预置浏览历史和同店购物车商品。
- 保留订单 `ORD202603300001`、`ORD202603300002`。
- 售后状态覆盖 `submitted`、`merchant_approved`、`processing`、`completed`。

### 2.3 演示环境建议

推荐按 README 中的 Tailscale 演示拓扑：

- 前端、后端、Rasa、Redis、PostgreSQL、Ollama、vLLM 都运行在同一台 Windows 主机。
- 远端设备只访问 `http://<本机 Tailnet IP>:5173`。
- 后端访问内部服务仍使用 `127.0.0.1`，不要把 Ollama、Redis、PostgreSQL 暴露到 Tailnet。
- `backend/.env` 和 `rasa/.env` 中的 `FRONTEND_BASE_URL` 需要设置成远端可访问的前端地址。

## 3. 演示账号与核心数据

### 3.1 演示账号

| 角色 | 邮箱 | 密码 | 演示用途 |
| --- | --- | --- | --- |
| 客户 | `test1@example.com` | `password123` | 推荐、购物车下单、兼容订单查询、物流查询 |
| 客户 | `test2@example.com` | `password123` | 已签收订单、售后全状态、历史投诉 |
| 商家 | `merchant1@example.com` | `password123` | 数码店订单、待处理售后 |
| 商家 | `merchant3@example.com` | `password123` | 办公店售后处理演示 |
| 商家 | `merchant4@example.com` | `password123` | 户外店售后处理中、已完成状态演示 |

### 3.2 关键订单

| 订单号 | 用户 | 状态定位 | 演示用途 |
| --- | --- | --- | --- |
| `ORD202603300001` | `test1@example.com` | 待发货 | 订单查询、修改地址、取消订单、售后草稿 |
| `ORD202603300002` | `test1@example.com` | 运输中 / 有历史售后 | 物流查询、售后进度查询 |
| `ORD202604010001` | `test2@example.com` | 已签收 | 退换货申请演示 |
| `ORD202604010002` | `test2@example.com` | 已签收 / 售后已提交 | 售后状态查询 |

### 3.3 核心话术

推荐话术：

```text
推荐一台适合写论文和轻量开发的银色笔记本，预算 6000 以内。
```

基于浏览历史推荐：

```text
我最近看了显示器和笔记本，帮我推荐适合毕业设计答辩准备的商品。
```

购物车下单：

```text
帮我把购物车里的商品下单，地址：北京市海淀区中关村软件园二期 8 号楼，邮箱：test1@example.com
```

订单查询：

```text
帮我查询订单 ORD202603300001 的状态。
```

物流查询：

```text
查询订单 ORD202603300002 的物流进度。
```

售后查询：

```text
查询订单 ORD202603300002 的售后进度。
```

售后申请：

```text
申请退货 ORD202603300001，原因：尺寸不合适。
```

## 4. 10-15 分钟演示时间轴

| 时间 | 演示任务 | 页面 / 账号 | 重点证明 |
| --- | --- | --- | --- |
| 0:00-1:00 | 项目定位与架构说明 | 口播 + 首页 | 系统不是单一聊天机器人，而是电商业务闭环 |
| 1:00-3:00 | 用户登录、商品浏览、本地图片展示 | `test1@example.com` / 商品页 | 本地演示数据完整，图片不依赖外链 |
| 3:00-5:00 | 客服商品推荐 | 客服页 | 推荐卡片、预算和用途约束 |
| 5:00-7:00 | 购物车下单草稿 | 客服页 + 购物车 | pending action 安全边界 |
| 7:00-9:00 | 订单、物流、售后查询 | 客服页 + 订单页 | 查询真实数据库中的订单与售后 |
| 9:00-11:00 | 售后申请草稿 | 客服页 | 模型生成草稿，用户确认后才执行 |
| 11:00-13:00 | 商家处理订单和售后 | `merchant1@example.com` / 商家工作台 | 商家端闭环处理能力 |
| 13:00-15:00 | 技术亮点和实验结论 | 口播 | Rasa + LLM/Agent + LoRA + benchmark 价值 |

## 5. 详细演示步骤

### 5.1 项目定位与系统架构说明

操作：

- 打开前端首页或商品页。
- 简要说明系统由前端商城、FastAPI 后端、Rasa、LLM/Agent、PostgreSQL、Redis 和 benchmark 工程组成。

口播重点：

```text
本项目不是单独做一个聊天机器人，而是围绕电商客服场景实现了一套完整实验系统。用户可以真实浏览商品、下单、查订单、申请售后；客服链路同时使用 Rasa 的规则稳定性和 LLM/Agent 的复杂理解能力；商家端可以处理订单和售后，形成完整闭环。
```

预期效果：

- 老师能快速理解演示不是只看聊天窗口，而是看一个业务系统和智能客服协同运行。

### 5.2 用户登录、商品浏览和本地图片展示

操作：

1. 打开前端。
2. 使用 `test1@example.com` / `password123` 登录。
3. 进入商品列表。
4. 打开一个商品详情页。
5. 展示商品图、店铺 Logo、价格、标签、规格和库存。

口播重点：

```text
这里展示的是初始化脚本生成的中文演示数据。所有商品图和店铺 Logo 都来自前端 public 目录下的本地 demo-assets，不依赖外部图片网站，所以答辩演示时不会出现图片加载失败的问题。
```

预期效果：

- 商品卡片图片正常显示。
- 商品详情信息完整。
- 页面没有外链图片加载失败占位。

### 5.3 客服商品推荐

操作：

1. 进入智能客服页。
2. 输入推荐话术：

```text
推荐一台适合写论文和轻量开发的银色笔记本，预算 6000 以内。
```

3. 等待客服回复。
4. 展示返回的商品推荐卡片。

口播重点：

```text
推荐链路不是简单关键词匹配。后端会解析预算、颜色、使用场景等约束，再结合商品标签、规格、评分、销量和浏览历史生成推荐。推荐结果以商品卡片返回，前端可以直接跳转到商品详情。
```

预期效果：

- 客服返回 1 个或多个商品卡片。
- 推荐结果应贴合“笔记本、银色、6000 以内、论文和轻量开发”。
- 卡片中包含商品名称、价格、图片和推荐信息。

### 5.4 购物车下单草稿

操作：

1. 确认 `test1@example.com` 已登录。
2. 进入购物车，简单展示购物车已有预置商品。
3. 回到客服页，输入：

```text
帮我把购物车里的商品下单，地址：北京市海淀区中关村软件园二期 8 号楼，邮箱：test1@example.com
```

4. 展示客服返回的下单草稿卡片。
5. 展示确认和取消按钮。

口播重点：

```text
这里体现的是系统安全边界。模型不会直接创建订单，而是生成待确认动作卡片。用户必须点击确认后，后端才会再次校验购物车、店铺、库存、地址和邮箱，并执行真实下单。
```

预期效果：

- 客服返回 `pending_action` 下单草稿卡片。
- 页面展示确认和取消动作。
- 草稿中能看到商品、金额、地址、邮箱等摘要。

建议：现场不一定点击确认。如果需要保留演示数据稳定性，可以只展示草稿和按钮，说明确认后才会落库。

### 5.5 订单查询、物流查询和售后进度查询

操作：

1. 在客服页输入：

```text
帮我查询订单 ORD202603300001 的状态。
```

2. 展示订单查询卡片。
3. 输入：

```text
查询订单 ORD202603300002 的物流进度。
```

4. 展示物流信息。
5. 输入：

```text
查询订单 ORD202603300002 的售后进度。
```

6. 展示售后状态。

口播重点：

```text
订单、物流和售后不是模型编造，而是从 PostgreSQL 中读取真实初始化数据。Rasa 和 Agent 都通过后端受控接口获取业务摘要，避免直接访问数据库，也避免模型绕过权限。
```

预期效果：

- `ORD202603300001` 能返回待发货订单信息。
- `ORD202603300002` 能返回物流进度。
- `ORD202603300002` 能返回历史售后进度。

### 5.6 售后申请草稿

操作：

1. 在客服页输入：

```text
申请退货 ORD202603300001，原因：尺寸不合适。
```

2. 展示售后申请草稿卡片。
3. 展示确认和取消按钮。

口播重点：

```text
售后申请同样采用待确认机制。LLM 负责理解用户意图并生成草稿，后端负责校验订单归属、订单状态、售后类型和是否允许申请。只有用户确认后，售后记录才会写入数据库。
```

预期效果：

- 返回售后 `pending_action` 卡片。
- 卡片展示订单号、售后类型、原因和有效时间。
- 页面有确认和取消动作。

### 5.7 商家工作台处理订单和售后

操作：

1. 退出客户账号或打开无痕窗口。
2. 使用 `merchant1@example.com` / `password123` 登录。
3. 进入商家工作台。
4. 查看店铺信息、商品列表、订单列表。
5. 展示待发货订单。
6. 切换到售后列表，展示待处理售后。
7. 如现场环境允许，执行“同意”或“处理中”等售后状态流转。

口播重点：

```text
商家端说明系统不是只有用户侧客服，还能形成商家处理闭环。用户端生成的订单、售后和投诉最终会进入商家工作台，商家可以发货、推进物流、处理售后。
```

预期效果：

- 商家账号只能看到自己店铺的数据。
- 商家订单页能看到待发货订单。
- 售后页能看到待处理售后项。
- 售后状态按钮可以按状态流转。

### 5.8 技术亮点和实验价值总结

操作：

- 回到系统页面或答辩 PPT。
- 用 1-2 分钟总结系统架构和 benchmark 结论。

口播重点：

```text
系统采用混合客服架构：Rasa 负责高频、稳定、结构化流程；LLM/Agent 负责复杂理解、推荐、多轮问答和多模态扩展；PostgreSQL 保存业务事实；Redis 加速缓存、锁和 pending action；模型不能直接写数据库，所有高风险动作都必须经过用户确认。
```

```text
benchmark 的结论是，LoRA 在推荐扩展场景上出现正向信号，但不能简单说成全局增强。纯 Rasa 在规则流程上稳定，但不适合承担复杂推荐和多模态任务。因此当前系统采用 Rasa + LLM/Agent 的混合方案。
```

预期效果：

- 老师能理解技术路线选择的原因。
- 演示从业务功能收束到架构价值和实验价值。

## 6. 关键讲解点

### 6.1 为什么不是纯聊天机器人

需要强调：

- 系统有真实业务表和真实业务状态。
- 客服只是入口之一，后端仍然负责权限和事务。
- 用户侧和商家侧形成闭环。

### 6.2 为什么需要 Rasa + LLM/Agent

说明分工：

- Rasa：适合意图固定、流程稳定、字段明确的任务。
- LLM/Agent：适合复杂推荐、自然语言理解、多轮上下文和知识融合。
- 后端路由层：决定使用规则链路还是 Agent 链路，并记录路由元数据。

### 6.3 为什么模型不能直接写库

说明安全边界：

- 下单、售后、取消订单、修改地址、物流投诉都属于高风险写操作。
- 模型只能生成草稿，不能直接执行。
- 用户确认后，后端重新校验权限、状态和参数，再执行写入。

### 6.4 为什么使用本地演示图片

说明演示稳定性：

- 旧方案如果依赖外链图片，答辩现场可能因为网络或站点限制导致图片加载失败。
- 当前所有种子商品和店铺 Logo 都来自 `frontend/public/demo-assets/`。
- 前端也有本地默认商品图兜底。

### 6.5 benchmark 如何支撑结论

说明边界：

- benchmark 不作为现场主演示内容。
- 它用于证明不同方案的能力边界。
- 当前结论是 LoRA 在推荐扩展上有正向信号，但不能夸大成所有场景都提升。

## 7. 异常兜底方案

### 7.1 LLM 响应较慢

处理方式：

- 说明本地模型推理受机器性能和模型大小影响，复杂推荐和 Agent 链路比规则链路更慢。
- 先切到商品页、订单页或商家工作台继续演示业务闭环。
- 等回复返回后再展示卡片。

口播兜底：

```text
这里使用的是本地模型服务，复杂推理会比普通接口慢。系统前端已经做了等待态和较长超时，真实业务数据页面不依赖模型，可以继续操作。
```

### 7.2 Rasa 未响应

处理方式：

- 直接演示后端 REST 业务页面：商品、购物车、订单、商家工作台。
- 说明后端是统一入口，Rasa 是规则能力层，不是唯一业务入口。

口播兜底：

```text
Rasa 负责规则型客服链路。如果 Rasa 服务不可用，业务 API 本身仍然可运行，后端也可以通过 Agent 或业务页面继续完成核心流程。
```

### 7.3 远端访问异常

处理方式：

- 改用本机浏览器访问 `http://localhost:5173` 或 `http://127.0.0.1:5173`。
- 不临时修改后端、Rasa、Ollama、Redis、PostgreSQL 的地址。

口播兜底：

```text
远端访问只影响展示终端，不影响系统内部服务。所有内部服务都在本机通过 127.0.0.1 通信，切回本机浏览器即可继续演示。
```

### 7.4 数据状态被现场操作改变

处理方式：

- 重新执行初始化脚本恢复演示数据：

```powershell
cd backend
.\scripts\init_postgres.ps1
```

注意：

- 该脚本会重建业务表，只用于本地演示环境。
- 执行后重新登录账号即可恢复预置购物车、订单和售后数据。

## 8. 演示验收标准

演示结束时应至少完成以下验收点：

- 商品列表和商品详情图片正常显示。
- 客户账号 `test1@example.com` 可以登录。
- 商品推荐话术返回商品推荐卡片。
- 购物车下单话术返回待确认动作卡片和确认/取消按钮。
- `ORD202603300001` 订单查询返回真实订单信息。
- `ORD202603300002` 物流查询返回真实物流进度。
- `ORD202603300002` 售后查询返回真实售后进度。
- 售后申请话术返回待确认动作卡片。
- 商家账号 `merchant1@example.com` 可以登录。
- 商家工作台能看到订单和售后处理项。
- 讲解中明确说明模型不能直接写库，写操作必须由用户确认。

如果时间不足，优先保留以下 5 个动作：

1. 商品浏览和本地图片展示。
2. 客服推荐商品卡片。
3. 客服生成下单待确认卡片。
4. 订单/物流/售后查询。
5. 商家工作台查看订单和售后。

## 9. 结尾总结话术

可直接使用以下总结：

```text
本系统围绕电商客服场景实现了用户端、客服端和商家端的完整闭环。用户可以浏览商品、咨询推荐、下单、查订单、查物流和申请售后；商家可以管理商品、处理订单和售后。
```

```text
技术上，系统采用 Rasa + LLM/Agent 的混合架构。Rasa 负责稳定的规则流程，LLM/Agent 负责复杂理解、推荐和多模态扩展。PostgreSQL 保存业务事实，Redis 提供缓存和锁，所有高风险写操作都通过 pending action 机制要求用户显式确认。
```

```text
实验上，benchmark 表明 LoRA 在推荐扩展能力上出现正向信号，但还不能夸大为全局增强。因此当前系统保留 Rasa 的稳定流程控制能力，同时把 LLM/Agent 用在更适合发挥理解和生成能力的场景中。
```

```text
从演示结果看，系统已经可以完成推荐、下单草稿、订单查询、物流查询、售后申请和商家处理售后的全链路展示，满足毕业设计演示要求。
```
